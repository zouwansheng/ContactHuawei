<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zws.ble.contacthuawei">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="login.LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="contactui.MainContactActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name="insert.AlreadyInsertActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name="edit.EditActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name="query.QueryListActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name="edit.TagActivity" />
        <activity android:name="linkaddress.LinkAddressActivity" />
        <activity android:name="linkaddress.AddMessageActivity" />
        <activity android:name="bean.kotlin.KotActivity"></activity>
    </application>

</manifest>